[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
playwright = "*"
psycopg-binary = "*"
torch = ">=2.0.0"
transformers = ">=4.30.0"
langchain = ">=0.1.0"
langchain-community = ">=0.0.10"
langchain-openai = ">=0.0.5"
langchain-huggingface = ">=0.0.5"
openai = ">=1.0.0"
gradio = ">=4.0.0"
fastapi = ">=0.100.0"
uvicorn = ">=0.20.0"
neo4j = ">=5.0.0"
sqlalchemy = ">=2.0.0"
alembic = ">=1.10.0"
asyncpg = ">=0.29.0"
psycopg = ">=3.1.0"
psycopg2-binary = ">=2.9.0"
requests = ">=2.30.0"
aiohttp = ">=3.8.0"
websockets = ">=10.0"
python-multipart = ">=0.0.5"
pytest = ">=7.0.0"
pytest-asyncio = ">=0.20.0"
pytest-cov = ">=4.0.0"
selenium = ">=4.0.0"
chromedriver-autoinstaller = ">=0.5.0"
numpy = ">=1.20.0"
pandas = ">=2.0.0"
pydantic = ">=2.0.0"
python-dotenv = ">=1.0.0"
structlog = ">=23.0.0"
tenacity = ">=8.0.0"
networkx = ">=3.0"
matplotlib = ">=3.5.0"
plotly = ">=5.0.0"
black = ">=23.0.0"
isort = ">=5.0.0"
mypy = ">=1.0.0"
pylint = ">=2.17.0"
pytest-watch = ">=4.2.0"

[dev-packages]
black = ">=23.0.0"
isort = ">=5.0.0"
mypy = ">=1.0.0"
pylint = ">=2.17.0"
pytest-watch = ">=4.2.0"

[requires]
python_version = "3.9"

[scripts]
test = "pytest"
lint = "pylint nova_aegis tests"
format = "black ."
sort = "isort ."
typecheck = "mypy ."
dev = "uvicorn nova_aegis.web.app:app --reload"
prod = "uvicorn nova_aegis.web.app:app --workers 4"
